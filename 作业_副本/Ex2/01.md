## (a)题目：标量乘法函数
```r
ex01Multiply <- function(x, y) {
  print(x * y)
}

ex01Multiply(2, 3)
```

## (b)题目：向量乘法函数
```r
ex02MultiplyVectors <- function(x, y) {
  x * y
}

ex02MultiplyVectors(c(1, 2), c(1, 2))
```