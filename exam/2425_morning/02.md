输入一个矩阵 mat，返回一个矩阵，移除了行和为负的行

```r
removeNegativeSumRows <- function(mat) {
  # your code
  checkmate::assert_matrix(mat, mode = "numeric", any.missing = FALSE)
  result <- mat[rowSums(mat) >= 0, , drop = FALSE]
  result
}
```