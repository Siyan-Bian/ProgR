输入一个向量，长度若小于 2 则返回 `0`，结果返回向量中索引为偶数的数值总和

```r
skipSum <- function(x) {
  # your code
  checkmate::assert_numeric(x, any.missing = FALSE)
  
  if (length(x) < 2) {
    return(0)
  }
  
  sum(x[seq(2, length(x), by = 2)])
}
```
